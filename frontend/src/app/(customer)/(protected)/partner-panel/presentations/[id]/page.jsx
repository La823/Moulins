// Reuses the standalone /presentations/[id] builder inside the panel's
// sidebar shell — same page everyone else still reaches via the navbar.
export { default } from "../../../presentations/[id]/page";
