// The panel's dashboard/overview tab reuses the exact same content as the
// standalone /dashboard page — no need to duplicate the fetch/render logic,
// the sidebar shell in layout.jsx is the only thing added on top here.
export { default } from "../dashboard/page";
