// Reuses the standalone /presentations page content inside the panel's
// sidebar shell — presentations shown here are the owning partner's,
// resolved server-side.
export { default } from "../../presentations/page";
