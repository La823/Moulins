// Reuses the standalone /doctors page content inside the panel's sidebar
// shell — same page everyone else still reaches via the navbar/dashboard.
// Doctors shown here are the owning partner's, resolved server-side.
export { default } from "../../doctors/page";
