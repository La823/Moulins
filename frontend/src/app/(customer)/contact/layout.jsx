export const metadata = {
  title: "Contact Us",
  description: "Get in touch with Moulins Pharmaceuticals — reach out for queries, partnerships, or support.",
  alternates: { canonical: "/contact" },
};

export default function ContactLayout({ children }) {
  return children;
}
